Authentication :
      JWT
      Refresh token rotation
      HTTP-only cookies

Password Security :
      bcrypt
      Never store plain passwords

API Security :
      Helmet
      CORS
      Input validation
      Rate limiting
      Error handling

Database Security :
      Environment variables
      MongoDB authentication
      Least privilege

Secrets :

Never commit:

     .env
     JWT secrets
     MongoDB credentials
     API keys
     private keys