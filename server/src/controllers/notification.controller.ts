import { Response } from "express";
import { AuthenticatedRequest } from "../middleware/auth.middleware.js";
import { getUnreadCountService, listNotificationsService, markAllNotificationsReadService, markNotificationReadService } from "../services/notification.service.js";
const userId = (req: AuthenticatedRequest) => { if (!req.user) throw new Error("Authentication required"); return req.user.userId; };
export const listNotificationsController = async (req: AuthenticatedRequest, res: Response) => { try { const result = await listNotificationsService(userId(req), req.query as never); res.json({ success: true, data: result.items, pagination: result.pagination }); } catch (error) { res.status(400).json({ success: false, message: error instanceof Error ? error.message : "Failed to fetch notifications" }); } };
export const unreadCountController = async (req: AuthenticatedRequest, res: Response) => { try { res.json({ success: true, data: { count: await getUnreadCountService(userId(req)) } }); } catch (error) { res.status(401).json({ success: false, message: error instanceof Error ? error.message : "Authentication required" }); } };
export const markNotificationReadController = async (req: AuthenticatedRequest, res: Response) => { try { const id = req.params.id; if (typeof id !== "string") throw new Error("Invalid notification ID"); const notification = await markNotificationReadService(id, userId(req)); res.json({ success: true, data: notification }); } catch (error) { res.status(404).json({ success: false, message: error instanceof Error ? error.message : "Notification not found" }); } };
export const markAllNotificationsReadController = async (req: AuthenticatedRequest, res: Response) => { try { const result = await markAllNotificationsReadService(userId(req)); res.json({ success: true, data: { modifiedCount: result.modifiedCount } }); } catch (error) { res.status(401).json({ success: false, message: error instanceof Error ? error.message : "Authentication required" }); } };
